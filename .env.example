# Public base URL of your deployed server (no trailing slash).
# Set this on your hosting platform (Railway, Render, Fly.io, etc.)
# If omitted the server infers it from the incoming request host.
BASE_URL=https://your-app.railway.app

# Port (most platforms set this automatically via $PORT)
PORT=3000
