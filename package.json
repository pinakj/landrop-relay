{
  "name": "landrop-relay",
  "version": "1.0.0",
  "description": "LANDrop relay server — temporary file hosting for cross-network transfers.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18"
  }
}
